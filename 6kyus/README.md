# CodeWars
Give two arrays that have all the letters and how it should be done 
Split the word into after the word 
then each word loop and the word change the word having the from the page and push it into the array
Then join
    assert.strictEqual(decode("yvvi"), "beer")
    assert.strictEqual(decode("Blf zoivzwb szw 10 yvvih"), "You already had 10 beers")
    assert.strictEqual(decode("Ovg'h hdrn rm gsv ulfmgzrm!"), "Let's swim in the fountain!")
    assert.strictEqual(decode("Tl slnv, blf'iv wifmp"), "Go home, you're drunk") 